name: Hourly Scraper
on:
  schedule:
    - cron: '0 * * * *'
  workflow_dispatch:

jobs:
  cron:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Scraper
        run: curl -X GET https://atlasconcours.vercel.app/api/cron-scraper
